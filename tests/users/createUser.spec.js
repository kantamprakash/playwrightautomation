const { test } = require('../../src/fixtures/baseTest');

// JavaScript port of com.krn.actitime.users.CreateUser
test('testCreateUser', { tag: ['@user', '@functionality', '@smoke'] }, async () => {
  console.log('Create User');
});
